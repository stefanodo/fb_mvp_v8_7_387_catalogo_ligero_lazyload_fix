fastapi==0.115.0
uvicorn==0.30.6
jinja2==3.1.4
python-multipart==0.0.9
psycopg2-binary==2.9.9
python-dotenv==1.0.1
pillow==11.1.0

# Exclude heavy optional packages (paddleocr, paddlepaddle, paddleocr>=2.7,<3, pytesseract)
# Add additional packages later if needed. Use `requirements.full.txt` for local development.
