DDL Sweep Report
Date: 2026-05-28

Summary
- Goal: locate runtime DDL (CREATE TABLE / CREATE INDEX / PRAGMA / AUTOINCREMENT / sqlite_master) so we can centralize into `backend/migrate.py` and remove SQLite-only runtime DDL from service code.

High-level findings
- Many DDL statements are already centralized in `backend/migrate.py` (multiple `CREATE TABLE IF NOT EXISTS` and `CREATE INDEX IF NOT EXISTS`).
- Remaining runtime DDL and SQLite-specific pragmas were found primarily in these files (recommended to move):
  - backend/app/core.py — multiple `CREATE TABLE IF NOT EXISTS ... AUTOINCREMENT` blocks, `PRAGMA table_info(...)`, `SELECT FROM sqlite_master` checks.
  - backend/app/db_config.py — SQLite `PRAGMA` setup executed at runtime (journal_mode, synchronous, busy_timeout).
  - backend/app/recipe_ai/storage_service.py — `conn.execute("PRAGMA foreign_keys = ON")`.
  - app/tools/corregir_hierbas_preparaciones.py — uses `PRAGMA table_info(...)` (tool script).
  - backend/app/pg_adapter.py — contains checks for tokens (`AUTOINCREMENT`, `PRAGMA`, `SQLITE_MASTER`) and logic to skip SQLite-only DDL when connected to Postgres (keep as-is).

Notes / false positives
- `PRAGMA` matches in `backend/app/main.py` and `backend/app/templates/index.html` are not DB pragmas (HTTP header / HTML meta `Pragma`) and can be ignored.

Detailed occurrences (examples)
- backend/migrate.py: centralized schema (many `CREATE TABLE IF NOT EXISTS` entries) — see [backend/migrate.py](backend/migrate.py)
- backend/app/core.py:
  - PRAGMAs: `for pragma in ["PRAGMA journal_mode=WAL", "PRAGMA synchronous=NORMAL", "PRAGMA busy_timeout=240000"]` (line ~89)
  - sqlite_master check: `SELECT 1 FROM sqlite_master WHERE type='table' AND name=?` (line ~248)
  - Many `CREATE TABLE IF NOT EXISTS ... id INTEGER PRIMARY KEY AUTOINCREMENT,` blocks starting around line ~1548 and later (multiple table definitions).
  See [backend/app/core.py](backend/app/core.py)
- backend/app/db_config.py: PRAGMA setup (journal_mode/WAL, synchronous, busy_timeout) — see [backend/app/db_config.py](backend/app/db_config.py)
- backend/app/recipe_ai/storage_service.py: `PRAGMA foreign_keys = ON` — see [backend/app/recipe_ai/storage_service.py](backend/app/recipe_ai/storage_service.py)
- app/tools/corregir_hierbas_preparaciones.py: fallback uses `PRAGMA table_info(items)` — see [app/tools/corregir_hierbas_preparaciones.py](app/tools/corregir_hierbas_preparaciones.py)

Recommendations (next steps)
1. Move all `CREATE TABLE` / `CREATE INDEX` / `PRAGMA` / AUTOINCREMENT usages from service modules into `backend/migrate.py` (idempotent `CREATE ... IF NOT EXISTS`), keeping `backend/migrate.py` as the single source of runtime schema.
2. Replace `AUTOINCREMENT` usage with Postgres-compatible sequences or use database-agnostic migration helpers; keep `migrate.py` responsible for producing correct SQL per engine.
3. Consolidate PRAGMA settings: run SQLite pragmas only when using SQLite (e.g., inside `db_config.py` or `core.db()`), and ensure the Postgres adapter does not execute them (or they are gated by `if not is_postgres:`).
4. For small one-off tools/scripts (e.g., in `app/tools/`) consider using helper functions that call into the central DB helper instead of raw `PRAGMA` usage.

Proposed immediate actions I can take next
- Move the `CREATE TABLE` blocks from `backend/app/core.py` into `backend/migrate.py` and remove duplicates (conservative, idempotent change). — safe and high priority.
- Move PRAGMA execution from `core.py` into `db_config.py` and gate by DB type.

If you'd like, I can: (pick one)
- Create a PR that extracts the `CREATE TABLE` blocks from `backend/app/core.py` into `backend/migrate.py` (automated move + tests), or
- Implement a smaller change: move only PRAGMA calls into `db_config.py` and gate them by SQLite.

---
Generated by automated DDL sweep.
